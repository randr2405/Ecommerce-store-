export async function GET() { const merchantId = process.env.PAYFAST_MERCHANT_ID; const merchantKey = process.env.PAYFAST_MERCHANT_KEY; const passphrase = process.env.PAYFAST_PASSPHRASE; const siteUrl = process.env.NEXT_PUBLIC_SITE_URL; return Response.json({ merchantId: merchantId || "MISSING", merchantKey: merchantKey ? "EXISTS" : "MISSING", passphrase: passphrase ? "EXISTS" : "MISSING", siteUrl: siteUrl || "MISSING" }); }
